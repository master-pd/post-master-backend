# ============================================
# POST-MASTER BACKEND - ENVIRONMENT TEMPLATE
# ============================================
# Copy this file to .env and fill with your values
# ============================================

# Server Configuration
NODE_ENV=development
PORT=5000
API_VERSION=v1
FRONTEND_URL=http://localhost:3000

# PostgreSQL Database (Get from Neon.tech)
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# JWT Secrets (Generate your own!)
JWT_SECRET="generate_using: node -e \"console.log(require('crypto').randomBytes(64).toString('hex'))\""
JWT_REFRESH_SECRET="another_random_secret_here"
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# Cloudinary Configuration (Sign up at cloudinary.com)
CLOUDINARY_CLOUD_NAME="your_cloud_name_here"
CLOUDINARY_API_KEY="your_api_key_here"
CLOUDINARY_API_SECRET="your_api_secret_here"
CLOUDINARY_UPLOAD_PRESET_NAME="post_master"

# Redis Configuration (Get from Redis Cloud)
REDIS_HOST="your_redis_host_here"
REDIS_PORT=6379
REDIS_PASSWORD="your_redis_password_here"
REDIS_URL="redis://default:password@host:port"

# Email Configuration (Gmail with App Password)
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT=587
EMAIL_USER="your_email@gmail.com"
EMAIL_PASSWORD="your_app_specific_password_here"
EMAIL_FROM="your_email@gmail.com"

# Supabase (Optional - for future features)
SUPABASE_URL="your_supabase_project_url"
SUPABASE_ANON_KEY="your_supabase_anon_key"
SUPABASE_SERVICE_ROLE_KEY="your_supabase_service_role_key"

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# Logging
LOG_LEVEL=info

# Upload Limits
MAX_VIDEO_SIZE=104857600  # 100MB
MAX_IMAGE_SIZE=10485760   # 10MB

# ============================================
# HOW TO GET THESE VALUES:
# ============================================

# 1. DATABASE_URL:
#    - Go to https://neon.tech
#    - Create free account
#    - Create new project
#    - Copy connection string from dashboard
#    - Add ?sslmode=require at the end

# 2. JWT_SECRET:
#    - Run in terminal: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#    - Copy the output as your secret

# 3. Cloudinary:
#    - Sign up at https://cloudinary.com
#    - Get API keys from dashboard
#    - Create upload preset named "post_master"

# 4. Redis:
#    - Go to https://app.redislabs.com
#    - Create free database
#    - Get connection details

# 5. Email:
#    - Use Gmail with 2FA enabled
#    - Generate App Password: https://myaccount.google.com/apppasswords
#    - Select "Mail" and "Other", name it "Post-Master"

# 6. Render.com Deployment:
#    - Add all these variables in Render dashboard
#    - Use actual values, not placeholders
# ============================================